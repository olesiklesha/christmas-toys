import React from 'react';
import { Container } from './styles';
import { Tree } from '../index';

const TreeField = () => {
  return (
    <Container i={1}>
      <Tree i={1} />
    </Container>
  );
};

export default TreeField;
